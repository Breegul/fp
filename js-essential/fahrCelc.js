const prompt = require("prompt-sync")({sigint: true});

let type = prompt("Would you like to start with celsius (C) or Fahrenheit (F)");

let base = undefined;

if(type === "C"){

} else if (type === "F"){

} else {

}